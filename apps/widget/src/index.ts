/**
 * @dtf/widget — the embeddable React + R3F fit experience (Web Component
 * `<dtf-fit>` + iframe fallback). Runs @dtf/fit-core client-side. See
 * docs/blueprint.md §1.1, §2 and CLAUDE.md.
 *
 * SCAFFOLD: Vite + React + R3F wiring (stage, metric HUD, body sheet, size
 * switch) is built in the Widget-app slice.
 */

export const WIDGET_APP = '@dtf/widget' as const;
