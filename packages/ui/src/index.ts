/**
 * @dtf/ui — shared design system: Tailwind tokens + headless primitives used by
 * the widget and console. Pairs with the Tailwind preset in @dtf/config.
 *
 * SCAFFOLD: React primitives are added alongside the Widget-app slice.
 */

export const UI_PACKAGE = '@dtf/ui' as const;
