/**
 * @dtf/sdk — public TypeScript client for tenants (REST/GraphQL over the API).
 * Published to npm; see docs/blueprint.md §1.1.
 *
 * SCAFFOLD: the typed client is generated/implemented in the API + DB slice.
 */

export const SDK_PACKAGE = '@dtf/sdk' as const;
